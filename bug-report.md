# Bug Report — income.html / income-v2.html

> **狀態：5 項全部已修正並部署**（2026-08-13）。修正內容與驗證結果見文末〈修正紀錄〉。
> 報告本身保留原樣，方便日後對照。

測試方式：讀完整原始碼 → 用 Node.js 重建 `simulate()` 演算法做數值驗證 → 在瀏覽器實際載入兩份檔案、操作滑桿與切換選項，交叉比對表格數字、圖表與結語文案是否一致。

核心的 12 個月模擬邏輯（小數進位 carry 機制、回購池 80% 回流與鎖價、15/45/90 階段門檻、耗材額度扣抵、圖表負值繪製）皆驗證正確，未發現問題。問題集中在 income-v2.html 新增成本/利潤功能時的接線疏漏。

## 摘要

| # | 檔案:行號 | 嚴重度 | 一句話 |
|---|---|---|---|
| 1 | income-v2.html:616 | **高（Critical）** | 「越過正職參考線」的月份判斷用到尚未算出的當月利潤，永遠晚報一個月，最壞情況會整個吃掉里程碑 |
| 2 | income-v2.html:458 | 低 | 圖表 `aria-label` 沒隨成本模式切換更新 |
| 3 | income-v2.html:447 | 低 | 「逐月推演」說明文字沒隨成本模式切換更新 |
| 4 | income-v2.html:682-688 | 極低 | `niceMax()` 是沿用 v1 的死程式碼，v2 已無呼叫點 |
| 5 | income-v2.html:708 | 極低 | 三元運算式兩分支回傳同值，形同虛設 |

---

## 1. `firstFullTime` 判斷使用未賦值的當月 profit（高／Critical）

**位置**：[income-v2.html:616](income-v2.html#L616)

**重現步驟**：不用改任何滑桿，直接開啟 income-v2.html（預設值 wk=10, md=6, ld=22, 達成率 80%，未開成本）。對照月度表第 7 列，利潤/收入欄已顯示 `NT$67,200`（已越過 6 萬正職線，該列 `data-hit="true"` 反白高亮）。

**預期結果**：結語應說「第 **7** 個月當月收入越過正職參考線」，與表格反白列一致。

**實際結果**：結語卻說「第 **8** 個月當月收入越過正職參考線」——比表格晚報一個月，同一畫面兩處自相矛盾。

再測一個更極端的情境（wk=10, md=8, ld=10, 達成率 100%，開成本、月租 4 萬）：月度表第 12 列（最後一列）利潤 `NT$60,967`，已越過 6 萬線、`data-hit="true"` 反白，但結語寫「整整一年的利潤都沒有越過正職參考線 NT$60,000」——把該有的里程碑**完全吃掉**。

**根因**：

```js
// 616 行：這裡讀 profit
if (profit >= A.fullTimeLine && firstFullTime === null) firstFullTime = m;
...
// 647 行：profit 到這裡才真正算出來
var profit = income - cost;
```

`var profit` 在函式作用域被提升（hoisted），616 行執行時它還沒被「這個月」賦值，讀到的其實是**上個月**留下的值（第一個月則是 `undefined`）。所以 `firstFullTime` 永遠對應「上上個月已經越線」的訊號，顯示出來就晚了一個月；如果越線剛好發生在第 12 個月，迴圈裡不會再有第 13 次迭代去讀到它，該次越線就會被永久漏掉，讀者看到的會是「整年都沒達標」的錯誤結論。

v1（income.html:493）沒有這個問題，因為它的判斷式 `if (income >= A.fullTimeLine ...)` 是放在 income 已經在同一個月完整算完之後才執行。

**建議修法**：把 616 行的判斷式移到 647 行 `var profit = income - cost;` **之後**（或併入 `out.push` 前的區塊），確保讀到的是當月剛算好的 profit。

**已用 Node 重建演算法驗證**（省略至此不重複貼程式，重點數字）：
- 預設參數下，profit 真正越線在第 7 個月；buggy 版回報第 8 個月。
- 搜尋參數空間可穩定找到「真正越線發生在第 12 個月、buggy 版回報 `null`（從未越線）」的情境，例如 wk=10, md=8, ld=10~18, 達成率 100%, 月租 4 萬。

---

## 2. 圖表 `aria-label` 沒隨成本模式更新（低）

**位置**：[income-v2.html:458](income-v2.html#L458)

**重現步驟**：點「加入成本」把圖表切成利潤模式（此時圖例粗體字、Y 軸都已正確改成「每月利潤」，並新增營業額虛線）。

**預期結果**：螢幕閱讀器唸出的圖表說明應同步反映「利潤／營業額雙線」。

**實際結果**：`<svg>` 的 `aria-label` 是寫死的靜態字串「十二個月**每月收入**折線圖，含正職參考線與第 3、6、12 個月里程碑」，全程式沒有任何 `setAttribute` 更新它，切換到利潤模式後仍唸成收入圖，也完全沒提到新增的營業額虛線。

**建議修法**：在 `render()` 或 `costBtn` 的 click handler 裡，依 `costsOn` 動態組字串並 `svg.setAttribute('aria-label', ...)`。

---

## 3. 「逐月推演」說明文字沒隨成本模式更新（低）

**位置**：[income-v2.html:447](income-v2.html#L447)

**重現步驟**：同上，切到成本模式後看「逐月推演」標題下方那行說明文字。

**預期結果**：應該說「每月**利潤**的走勢」。

**實際結果**：這段 `<p class="lede">` 沒有 `id`，JS 完全不會碰它，永遠停留在「每月**收入**的走勢」，跟旁邊已經正確切換的 KPI 卡、圖例、Y 軸文案不一致。

**建議修法**：給這個 `<p>` 加 `id`，在 `render()` 裡跟 `#legMain` 一起同步更新。

---

## 4. `niceMax()` 死程式碼（極低）

**位置**：[income-v2.html:682-688](income-v2.html#L682)

全文搜尋 `niceMax(` 只有定義、沒有任何呼叫點。v1 的 `drawChart` 靠它算 Y 軸上界，v2 改用 `niceStep` 整套新邏輯後，這個函式被複製過來但沒被清掉。

**建議修法**：刪除。

---

## 5. 三元運算式兩分支回傳同值（極低）

**位置**：[income-v2.html:708](income-v2.html#L708)

```js
var hiV = Math.max.apply(null, rows.map(function(x){ return costsOn ? x.income : x.income; }));
```

`costsOn ? x.income : x.income` 兩邊都回傳 `x.income`，判斷式形同虛設。目前因為 `profit` 恆 `≤ income`（成本不會是負數），拿 income 當 Y 軸上界剛好還算安全，沒有造成外顯錯誤，但明顯是複製貼上留下的殘餘，容易誤導之後維護的人以為這裡有特別處理。

**建議修法**：直接寫成 `x.income`，拿掉沒意義的三元判斷。

---

## 未發現問題、已驗證乾淨的部分

- `tierForNth` / `tierAt` 階段邊界（15 / 45 / 90）無 off-by-one
- `carryM` / `carryL` 小數累進機制在極端輸入下數值正確
- 回購池鎖價邏輯（半價客 ×0.9 = 1,800、全價客 ×0.9 = 3,600）與回購率 80% 回流正確
- v2 耗材額度扣抵（色乳 500 次 / 針頭 20 次用完才計價）跨月累計正確，`venueFreeSessions`（前 4 次不計場地）正確
- 廣告成本 = 營業額 × 1/7，半價客人廣告自然打半折，計算正確
- 圖表負值繪製（含 −5 萬刻度、0 基準線、里程碑負值標籤）在桌面寬度下渲染正確
- `trackPct` 幾何內插進度條在單排（未折行）情況下與階梯格子邊界對齊正確


---

# 修正紀錄（2026-08-13）

五項全部修正，`income.html`（v1）**未做任何變動**——經確認 v1 的判斷式（income.html:493）
本來就在 `income` 累加完之後，沒有這個問題；`niceMax()` 在 v1 有實際呼叫點，也沒有第 5 項。

## 報告漏掉的一項：辭職判定也踩到同一個坑

報告寫成之後才加了「辭職目標」功能，**辭職判定跟 `firstFullTime` 寫在同一個位置**
（原 684 行），同樣讀到上個月的 `profit`：

```js
if (quitOn && quitAt === null){
  if (profit >= target){ hitStreak += 1; if (hitStreak >= streak) quitAt = m; }
  else hitStreak = 0;
}
```

所以**辭職月也晚報一個月**，而且連續達標的計數（`hitStreak`）跟著錯位。
修正時一併移到 `profit` 算完之後。

> 這個錯誤其實在開發當下就有跡象：我用 Node 寫的原型算出辭職月是第 9 個月，
> 瀏覽器卻顯示第 10 個月。當時把差異歸因於設定不同而沒有追下去——那是漏掉的一步。

## 逐項處理

| # | 處理 | 驗證 |
|---|---|---|
| 1 | 把 `firstFullTime` 與辭職判定移到 `var profit = income - cost;` **之後**，並加註解說明為何不能往前搬 | 見下表 |
| 2 | `render()` 內依 `costsOn` / `quitOn` 動態組字串並 `svg.setAttribute('aria-label', …)` | 四種模式組合皆正確 |
| 3 | `<p class="lede">` 加上 `id="chartLede"`，跟 `#legMain`、`aria-label` 一起更新 | 同上 |
| 4 | 刪除 `niceMax()`（先確認 v2 內 `niceMax(` 出現次數為 1，即只有定義無呼叫） | 檔內已無殘留 |
| 5 | `costsOn ? x.income : x.income` 改為 `x.income`，並補註解說明為何上界看 income 就夠 | — |

## 第 1 項的數值驗證

用 Node 重建演算法，同時跑「舊行為（讀上個月 profit）」與「修正後」對照：

| 情境 | 表格實際越線月 | 舊版回報 | 修正後 |
|---|---|---|---|
| 預設值（wk10 md6 ld22 80%，未開成本） | 第 7 個月 | 第 8 個月 | **第 7 個月** |
| 報告的極端例（wk10 md8 ld10 100%，月租 4 萬） | 第 12 個月 | **未達標** | **第 12 個月** |
| 辭職目標（門檻 45,000 連續 2 月） | 第 9 個月 | 第 10 個月 | **第 9 個月** |

瀏覽器實測同步確認：預設值下表格反白列與結語都是「第 7 個月」；
極端例第 12 列利潤 NT$60,967、反白，結語也正確報第 12 個月，不再被吃掉。

## 回歸測試

4 種情境錨點 × 3 種達成率共 12 組全跑過：零 console 錯誤、無橫向溢出、
SVG 文字零重疊、觸控目標全部 ≥36px、進度條與階梯對齊正常。
